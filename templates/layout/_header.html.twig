<header>
    <nav>
        <ul class="d-flex align-items-center gap-5">
            <li>
                <a href="{{ path('app_home') }}">
                    <img src="{{ asset('build/images/logo.png') }}" alt="logo">
                </a>
            </li>
            <li>
                <a href="{{ path('app_deal_index') }}">Bons plans</a>
            </li>
            <li>
                <a href="{{ path('app_code_promo_index') }}">Codes promo</a>
            </li>
            <li class="ms-auto">
                {% if app.user %}
                    <a href="{{ path('app_user') }}">Mon compte</a>
                    <a href="{{ path('app_logout') }}">Déconnexion</a>
                {% else %}
                    <a href="{{ path('app_login') }}">Connexion</a>
                {% endif %}
            </li>
        </ul>
    </nav>
</header>
