<header>
    <nav>
        <ul class="d-flex align-items-center gap-5">
            <li>
                <a href="{{ path('app_home') }}">
                    <img src="{{ asset('build/images/logo.png') }}" alt="logo">
                </a>
            </li>
            <li>
                <a href="{{ path('app_deal_index') }}">Bons plans</a>
            </li>
            <li>
                <a href="{{ path('app_code_promo_index') }}">Codes promo</a>
            </li>
            <li class="ms-auto d-flex gap-5 align-items-center">
                <form action="{{ path('app_search') }}">
                    <div class="input-group mb-3">
                        <input type="text" class="form-control" name="search" placeholder="Rechercher" aria-label="Rechercher" aria-describedby="button-addon2" value="{{ search ?? null }}">
                        <button class="btn btn-outline-light" type="submit" id="button-addon2">Rechercher</button>
                    </div>
                </form>
                {% if app.user %}
                    <a href="{{ path('app_logout') }}">Déconnexion</a>
                {% else %}
                    <a href="{{ path('app_login') }}">Connexion</a>
                {% endif %}
            </li>
        </ul>
    </nav>
</header>
