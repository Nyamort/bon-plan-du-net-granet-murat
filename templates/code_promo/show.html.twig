{% extends 'base.html.twig' %}

{% block title %}CodePromo{% endblock %}

{% block body %}
    <h1>CodePromo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ code_promo.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ code_promo.code }}</td>
            </tr>
            <tr>
                <th>ExpiredAt</th>
                <td>{{ code_promo.expiredAt ? code_promo.expiredAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ code_promo.value }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_code_promo_index') }}">back to list</a>

    <a href="{{ path('app_code_promo_edit', {'id': code_promo.id}) }}">edit</a>

    {{ include('code_promo/_delete_form.html.twig') }}
{% endblock %}
